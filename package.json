{
  "name": "next-markdown-blog",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "set NODE_OPTIONS=--openssl-legacy-provider && next dev",
    "build": "NODE_OPTIONS=--openssl-legacy-provider && next build && next-sitemap",
    "start": "NODE_OPTIONS=--openssl-legacy-provider && next start",
    "lint": "next lint"
  },
  "dependencies": {
    "axios": "^1.6.7",
    "gray-matter": "^4.0.3",
    "marked": "^2.1.2",
    "next": "11.0.1",
    "next-sitemap": "^4.2.3",
    "react": "17.0.2",
    "react-dom": "17.0.2"
  },
  "devDependencies": {
    "eslint": "7.29.0",
    "eslint-config-next": "11.0.1"
  }
}
